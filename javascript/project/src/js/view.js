export default class View {
    constructor() {
        this.refs = {

        };

    }

    // createMarkup(list) {
    //     this.refs.###.innerHTML = "";
    //     const source = document.querySelector("#.....").innerHTML.trim();
    //     const template = Handlebars.compile(source);
    //     const markup = list.reduce((acc, bookmark) => acc + template(bookmark), "");
    //     this.refs.###.insertAdjacentHTML("beforeend", markup);
    // }
}